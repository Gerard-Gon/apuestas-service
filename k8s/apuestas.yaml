apiVersion: apps/v1
kind: Deployment
metadata:
  name: apuestas-deployment # ej: apuestas-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: apuestas
  template:
    metadata:
      labels:
        app: apuestas
    spec:
      containers:
      - name: apuestas
        image: 914091100375.dkr.ecr.us-east-1.amazonaws.com/apuestas-service:latest
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
        ports:
        - containerPort: 8005
        # CAMBIO: Inyectamos el host de la BD para que el código no busque localhost
        env:
        - name: DB_HOST
          value: "postgres-service"
        envFrom:
        - secretRef:
            name: casino-secrets
        # CAMBIO: Agregamos las sondas de salud que definiste en tu código
        livenessProbe:
          httpGet:
            path: /livez
            port: 8005
          initialDelaySeconds: 10
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8005
          initialDelaySeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: apuestas-service
spec:
  type: ClusterIP
  selector:
    app: apuestas 
  ports:
  - port: 8005
    targetPort: 8005